# «Догодок» — SaaS для работы с договорами через ИИ

> Этот файл — главный контекст проекта. Читай его целиком перед тем как начинать любую задачу.
> Обновляй раздел ЗАДАЧИ когда берёшь задачу в работу или завершаешь её.

---

## Что за продукт

Коммерческий SaaS-сервис для работы с договорами через ИИ.

Пользователи могут:
- Создавать договоры, приложения, допсоглашения с нуля через ИИ
- Загружать готовые документы и проверять их на риски
- Сравнивать версии документов
- Редактировать через ИИ-чат и вручную в предпросмотре
- Хранить контрагентов с реквизитами и подписантами
- Предоплачивать ИИ-действия токенами (генерация 100, правки загруженного 50, проверка 25 и т.д. — `lib/token-pricing.ts`)
- Скачивать любые свои версии в Word бесплатно и без ограничений

**Целевая аудитория:** ИП, подрядчики, малые компании — те кто работает с договорами без юриста.

---

## Дизайн-макет

Лежит тут: `макет/design_handoff_dogovora/`

- Открой `source/Договора — Дизайн-концепт.html` в Chrome — все 20 экранов
- Скриншоты всех экранов: `screenshots/` (файлы 01.png — 22.png)
- Все цвета, шрифты, отступы: `source/tokens.css`
- Компоненты: `source/components.jsx`

**Реализовывать пиксель-в-пиксель по макету.**

---

## Стек технологий

| Что | Технология |
|-----|-----------|
| Frontend | Next.js 14 (App Router) + TypeScript |
| Стили | Tailwind CSS (токены из tokens.css → tailwind.config) |
| UI компоненты | shadcn/ui (кастомизированные под дизайн) |
| Глобальное состояние | Zustand |
| Запросы к API | TanStack Query |
| Формы и валидация | React Hook Form + Zod |
| Backend | Fastify + TypeScript |
| База данных | PostgreSQL через Prisma ORM |
| Очереди задач | BullMQ + Redis |
| Файлы | Локальный `STORAGE_DIR` (S3 — на будущее) |
| ИИ | GigaChat / Polza.ai / OpenRouter / mock; маршруты задач в БД или ENV (`AI_PROVIDER`) |
| Логи | Файлы `logs/app.log` + `logs/error.log` (JSONL ERROR; `LOG_*`; `docs/LOG_EVENTS.md`) |

---

## Структура проекта (монорепо)

```
dogovora/
├── apps/
│   └── web/          ← Next.js: UI + API routes + Prisma + worker
├── packages/
│   └── shared/       ← форматирование документов (DOCX-конвертер и т.п.)
├── CLAUDE.md         ← этот файл
├── ПРОМТЫ-ИИ.md      ← обзор ИИ-промптов и задач
└── макет/            ← дизайн-хэндоф (только для справки, не трогать)
```

---

## База данных — главные сущности

```
User → Wallet (баланс)
User.isAdmin → доступ к /admin/ai
User → Profiles (реквизиты: ИП / ООО / физлицо)
User → Counterparties (контрагенты)

Counterparty → BankDetails
Counterparty → Signatories (подписанты)
Counterparty → Documents

Document → Versions  ← APPEND-ONLY! Версии никогда не перезаписываются
Version → AISettings (уровень защиты, объём, инструкция)
Version → ChatMessages (история ИИ-чата этой версии)
Version → Purchase (legacy: покупки эпохи постоплаты, новые не создаются)

Wallet → Transactions (история операций; currency: RUB legacy | TOKEN)
User → TokenCharge (append-only списания токенов; пакеты правок документа)
Document → VersionDraft (рабочая копия несохранённых правок)

AIOperator → AITaskRoute (задача → оператор + модель)
AIOperator → AIUsageLog (расходы/токены)
```

---

## Конфигурация ИИ (после MVP)

- **Админка:** `/admin/ai` — только для `User.isAdmin`. Операторы (ключи), маршруты задач, расходы.
- **Операторы:** `gigachat`, `polza`, `openrouter`, `mock`. Ключи в БД шифруются (`AI_CREDENTIALS_ENCRYPTION_KEY` или `JWT_SECRET`).
- **Задачи:** `generate`, `edit`, `chat`, `quick_analysis`, `review`, `analyze_upload`, `extract_parties`, плюс служебные `spelling` / `review_fallback`. Запасной маршрут — `default`.
- **Приоритет:** маршрут задачи в БД → `default` в БД → ENV (`AI_PROVIDER`, `AI_MODEL_*`, ключи GigaChat/Polza/OpenRouter).
- **Код:** `apps/web/src/lib/ai/` (transport, config, tasks, providers), API `apps/web/src/app/api/admin/ai/`.
- **ПДн:** перед ИИ маскируются ИНН/счета и т.п. (`anonymize`); при правке блок реквизитов отрезается и возвращается после ответа; извлечение сторон с загрузки — только с `consentPii`.
- **Чат на рабочем экране:** режимы «Правка» / «Вопрос» + кнопка «Анализ».

Подробнее про тексты промптов: `ПРОМТЫ-ИИ.md` (источник истины — `gigachat-provider.ts`).

---

## Правила которые нельзя нарушать

1. **Версии — только добавлять.** Никогда не обновлять текст/файл существующей версии. Каждое изменение = новая версия.
2. **Файлы — путь в БД, бинарь на диске.** Не хранить файл в PostgreSQL. Сейчас — `STORAGE_DIR`, не S3.
3. **ИИ-запросы — в очередь.** Генерация документа через ИИ всегда идёт через BullMQ воркер, не в HTTP запросе напрямую.
4. **Списания токенов — транзакция БД.** Монетизация предоплатная: токены списываются за генерацию, пакеты правок, переписку, проверку и анализ (`lib/token-charges.ts`). Проверка баланса и списание атомарны (SELECT FOR UPDATE), таблица `token_charges` — append-only (возврат = `refundedAt` + CREDIT, не DELETE). Цены — только через `lib/token-pricing.ts` / ENV `TOKEN_PRICE_*`.
5. **ИИ-чат — постоянная колонка справа.** На рабочем экране чат не открывается поверх документа, он всегда рядом.
6. **Одна primary-кнопка на экран** (чёрная, главное действие).
7. **Интерфейс только на русском языке.**
8. **Админ-сиды с паролями в репозиторий не класть.** Админа на проде выдавать через `UPDATE … SET "isAdmin" = true`.
9. **Админ не видит документы пользователей.** `isAdmin` даёт доступ только к `/admin/ai` (операторы, маршруты, расходы). Ни один маршрут документов/версий не должен обходить скоуп по владельцу — всегда `where: { … userId }` / `where: { … document: { userId } }`. Роль владельца не заменяется ролью админа никогда.
10. **Согласия — только добавлять.** Таблица `user_consents` append-only, как и версии. Отзыв согласия — новая строка с `granted = false`, а не UPDATE. Вместе с решением фиксируются редакция документа (`LEGAL_VERSION`), дата, IP и user-agent — это единственное доказательство акцепта. Правки текста документов в `apps/web/src/lib/legal/` по существу требуют поднять `LEGAL_VERSION`; опечатки и оформление — нет.
11. **Содержимое договоров не попадает в логи.** В `console.*` и логгер идут только размеры, счётчики и коды ошибок. Запрещено логировать `Version.content`, `formattedContent`, сообщения чата, инструкции пользователя, ответы ИИ и фрагменты поиска (`failedSearches`) — логи читают админы и разработчики.

---

## Дизайн-система (кратко)

**Шрифты:**
- Заголовки: Source Serif 4
- Интерфейс: IBM Plex Sans
- Числа/реквизиты/версии: IBM Plex Mono

**Основные цвета:**
- Фон страницы: `#FAF8F3` (тёплый off-white)
- Карточки: `#FFFFFF`
- Основной текст: `#18181B`
- Акцент (ИИ, ссылки): slate-indigo `oklch(0.42 0.06 260)`

**Статусы документов:** Черновик / В работе / На проверке / Утверждено / Оплачено

Все токены подробно в `макет/design_handoff_dogovora/source/tokens.css`

---

## Что НЕ делаем в MVP

- ❌ Встроенный ручной редактор документа (только ИИ-чат)
- ❌ DaData / автозаполнение из ЕГРЮЛ (реквизиты вводятся вручную)
- ❌ OAuth через Госуслуги / Яндекс ID (но в схеме БД место для этого есть)
- ❌ Реальный платёжный шлюз (только внутренний баланс)
- ❌ Тёмная тема
- ❌ Командная работа / роли

---

## Как проверить что всё работает (e2e сценарий)

```
1. Зарегистрироваться / войти → на балансе 500 токенов
2. Заполнить свои реквизиты (ИНН, банк и т.д.)
3. Создать контрагента, добавить его реквизиты и подписанта
4. Создать новый договор → выбрать контрагента → настроить ИИ
5. Запустить генерацию → списывается 100 токенов (в цену входит 10 ИИ-правок)
6. Рабочий экран: документ без водяных знаков, копируется и редактируется
7. Через ИИ-чат внести правку (тратится пакет) → «Сохранить» → новая версия
8. Проверить документ на риски → списывается 25 токенов
9. Сравнить версию 1 и версию 2
10. Утвердить версию (бесплатно) → скачать DOCX (бесплатно, сколько угодно раз)
11. Проверить историю операций (токены) и хранилище
```

---

## ЗАДАЧИ

> ⚠️ Перед тем как взять задачу — проверь что она не в статусе 🔄 (в работе у другого).
> Когда берёшь задачу — поставь 🔄 и своё имя. Когда сделал — поставь ✅.

### Фаза 0 — Инфраструктура
| # | Задача | Статус |
|---|--------|--------|
| 0.1 | Создать монорепо (pnpm workspaces) со структурой apps/ и packages/ | ✅ |
| 0.2 | Настроить TypeScript strict + ESLint + Prettier | ✅ |
| 0.3 | docker-compose с PostgreSQL и Redis | ✅ |
| 0.4 | Создать .env.example с переменными DATABASE_URL, REDIS_URL, S3_*, AI_*, JWT_SECRET | ✅ |
| 0.5 | Инициализировать Prisma в packages/db/ | ✅ |
| 0.6 | Базовый CI (GitHub Actions: lint + typecheck) | ✅ |

### Фаза 1 — Дизайн-система
| # | Задача | Статус |
|---|--------|--------|
| 1.1 | Перенести tokens.css в tailwind.config.ts (цвета, радиусы, отступы) | ✅ |
| 1.2 | Подключить шрифты (Source Serif 4, IBM Plex Sans, IBM Plex Mono) | ✅ |
| 1.3 | Компонент Button (5 вариантов × 3 размера) | ✅ |
| 1.4 | Компонент Badge (7 статусов) | ✅ |
| 1.5 | Компонент Card | ✅ |
| 1.6 | Компоненты Input, Textarea, Select | ✅ |
| 1.7 | Компонент Slider (для ИИ-настроек) | ✅ |
| 1.8 | Компонент Avatar (инициалы) | ✅ |
| 1.9 | Компонент Tabs | ✅ |
| 1.10 | Компонент AppLayout (sidebar + topbar + content) | ✅ |
| 1.11 | Страница /ui-kit со всеми компонентами для проверки | ✅ |

### Фаза 2 — Авторизация
| # | Задача | Статус |
|---|--------|--------|
| 2.1 | Prisma модель User + OAuthAccount (для будущего OAuth) | ✅ |
| 2.2 | API: POST /auth/register, POST /auth/login, POST /auth/refresh | ✅ |
| 2.3 | JWT access token (15 мин) + refresh token в httpOnly cookie | ✅ |
| 2.4 | Экран входа/регистрации (01-auth.png) | ✅ |
| 2.5 | Защита маршрутов через Next.js middleware | ✅ |
| 2.6 | Zustand authStore (user, token) | ✅ |

### Фаза 3 — AppLayout и навигация
| # | Задача | Статус |
|---|--------|--------|
| 3.1 | Подключить реальный роутинг (все маршруты-заглушки) | ✅ |
| 3.2 | Sidebar с навигацией и кнопкой «Новый документ» | ✅ |
| 3.3 | Topbar с breadcrumb и балансом | ✅ |
| 3.4 | Zustand walletStore (balance) | ✅ |

### Фаза 4 — Реквизиты пользователя
| # | Задача | Статус |
|---|--------|--------|
| 4.1 | Prisma модель Profile | ✅ |
| 4.2 | API: CRUD /api/profiles | ✅ |
| 4.3 | Экран «Мои реквизиты» (03-my-requisites.png) | ✅ |
| 4.4 | Загрузка факсимиле и печати в S3 | 🚫 (отложено — S3 провайдер TBD) |
| 4.5 | Валидация ИНН (контрольная сумма), БИК, расчётного счёта | ✅ |

### Фаза 5 — Контрагенты
| # | Задача | Статус |
|---|--------|--------|
| 5.1 | Prisma модели: Counterparty, BankDetail, Signatory | ✅ |
| 5.2 | API: CRUD /api/counterparties | ✅ |
| 5.3 | API: CRUD /api/counterparties/:id/signatories | ✅ |
| 5.4 | Список контрагентов (05-parties.png) | ✅ |
| 5.5 | Карточка контрагента (06-party-card.png) | ✅ |
| 5.6 | Вкладка «Реквизиты» контрагента (18-party-requisites.png) | ✅ |
| 5.7 | Редактирование реквизитов (19-edit-requisites.png) | ✅ |
| 5.8 | Drawer подписанта (20-signatory-editor.png) | ✅ |

### Фаза 6 — Документы и версии
| # | Задача | Статус |
|---|--------|--------|
| 6.1 | Prisma модели: Document, Version | ✅ |
| 6.2 | API: POST /api/documents, GET /api/documents | ✅ |
| 6.3 | API: GET /api/documents/:id | ✅ |
| 6.4 | API: POST /api/documents/:id/versions (append-only!) | ✅ |
| 6.5 | Список документов (07-documents.png) | ✅ |
| 6.6 | Карточка документа с историей версий (08-document-overview.png) | ✅ |
| 6.7 | Мастер создания документа шаг 1 (09-create-document.png) | ✅ |
| 6.8 | Настройки ИИ шаг 2 (10-ai-settings.png) | ✅ |

### Фаза 7 — ИИ и рабочий экран ★
| # | Задача | Статус |
|---|--------|--------|
| 7.1 | Интерфейс AIProvider + mock-провайдер + GigaChat провайдер | ✅ |
| 7.2 | BullMQ воркер generate-document | ✅ |
| 7.3 | API: POST /api/versions/:id/generate (ставит задачу в очередь) | ✅ |
| 7.4 | API: GET /api/jobs/:jobId (статус задачи) | ✅ |
| 7.5 | API: SSE /api/versions/:id/chat (стриминг ответов ИИ) | ✅ |
| 7.6 | API: POST /api/versions/:id/chat (сохранить сообщение) | ✅ |
| 7.7 | API: GET /api/versions/:id/review (проверка через ИИ) | ✅ |
| 7.8 | ★ Главный рабочий экран (13-main-work-screen.png) | ✅ |
| 7.9 | Экран проверки документа (11-check-document.png) | ✅ |
| 7.10 | Экран сравнения версий (12-compare-versions.png) | ✅ |
| 7.11 | Интеграция GigaChat (провайдер, auth, stream, env-переключение) | ✅ |

### Фаза 8 — Баланс и оплата версий
| # | Задача | Статус |
|---|--------|--------|
| 8.1 | Prisma модели: Wallet, Transaction, Purchase | ✅ |
| 8.2 | API: GET /api/wallet, POST /api/wallet/topup | ✅ |
| 8.3 | API: POST /api/versions/:id/purchase (ACID транзакция) | ✅ |
| 8.4 | API: GET /api/wallet/transactions | ✅ |
| 8.5 | Экран баланса (04-balance.png) | ✅ |
| 8.6 | Модалка подтверждения покупки (15-purchase-confirm.png) | ✅ |
| 8.7 | История платежей (16-payment-history.png) | ✅ |

### Фаза 9 — Хранилище
| # | Задача | Статус |
|---|--------|--------|
| 9.1 | Prisma модель StorageQuota | ✅ |
| 9.2 | API: GET /api/storage | ✅ |
| 9.3 | Экран хранилища и тарифов (17-storage-and-plans.png) | ✅ |

### Фаза 10 — История и главная
| # | Задача | Статус |
|---|--------|--------|
| 10.1 | API: GET /api/history (все версии пользователя, фильтры) | ✅ |
| 10.2 | Экран истории версий (14-version-history.png) | ✅ |
| 10.3 | Главная страница (02-home.png) | ✅ |

### Фаза 11 — QA и полировка
| # | Задача | Статус |
|---|--------|--------|
| 11.1 | Empty states (пустые списки) | ✅ |
| 11.2 | Loading states (скелетоны) | ✅ |
| 11.3 | Error states (toast уведомления) | ✅ |
| 11.4 | Адаптивность ≤768px | ✅ |

### Фаза 13 — ИИ-редактирование документа (багфикс + доработка)
| # | Задача | Статус |
|---|--------|--------|
| 13.1 | AIProvider.editDocument() — стримит обновлённый текст документа | ✅ |
| 13.2 | Chat API: режим edit/chat, SSE-события {type:'doc'} и {type:'chat'} | ✅ |
| 13.3 | Рабочий экран: документ обновляется в реальном времени при чате | ✅ |
| 13.4 | Индикатор «ИИ обновляет документ…» + индикатор несохранённых правок | ✅ |
| 13.5 | saveAsNewVersion передаёт текущий текст документа в новую версию | ✅ |
| 13.6 | GET /api/versions/:id — эндпоинт для получения контента версии | ✅ |
| 13.7 | Сравнение версий: пословный diff (LCS) как в Word, загрузка реального контента | ✅ |

### Фаза 14 — Мультиоператор ИИ и админка
| # | Задача | Статус |
|---|--------|--------|
| 14.1 | Prisma: AIOperator, AITaskRoute, AIUsageLog + User.isAdmin | ✅ |
| 14.2 | Runtime: маршрут по задаче (БД → ENV), transport GigaChat/Polza | ✅ |
| 14.3 | Админка `/admin/ai`: операторы, маршруты, расходы | ✅ |
| 14.4 | Сайдбар «Настройки ИИ» для isAdmin | ✅ |
| 14.5 | Чат: режимы Правка / Вопрос + quick_analysis | ✅ |
| 14.6 | PiiGuard + согласие на extract_parties при загрузке | ✅ |
| 14.7 | Учёт токенов/расходов в usage-логе | ✅ |

### Фаза 15 — Правовые документы и согласия (152-ФЗ)
| # | Задача | Статус |
|---|--------|--------|
| 15.1 | Пакет из 5 документов в `apps/web/src/lib/legal/` (оферта, пользовательское соглашение, политика, согласие ПДн, согласие на трансграничную передачу) | ✅ |
| 15.2 | Публичные страницы `/legal` и `/legal/[slug]` + компонент рендера блоков | ✅ |
| 15.3 | Prisma: `UserConsent` + enum `ConsentKind`, миграция `add_user_consents` | ✅ |
| 15.4 | Чекбоксы согласий на форме регистрации + компонент `Checkbox` | ✅ |
| 15.5 | API регистрации: обязательные согласия, запись в той же транзакции | ✅ |
| 15.6 | `/api/consents` + окно принятия для аккаунтов, созданных до введения согласий | ✅ |
| 15.7 | Перенести новые редакции документов на маркетинговый сайт dogodoc.ru | ⬜ (вне репозитория, см. «Документы продукта/09-…») |
| 15.8 | Подать уведомление в Роскомнадзор о трансграничной передаче, выждать 10 рабочих дней | ⬜ (действие владельца) |
| 15.9 | Гейт маршрутизации ИИ-задач по согласию на трансграничную передачу | ⬜ |

### Фаза 16 — Предоплата токенами, редактируемый предпросмотр, слой оформления
> Спека: `docs/superpowers/specs/2026-08-19-token-prepaid-design.md`, план: `docs/superpowers/plans/2026-08-19-token-prepaid.md`

| # | Задача | Статус |
|---|--------|--------|
| 16.1 | Конфиг цен `token-pricing.ts` + тесты | ✅ |
| 16.2 | Prisma: TokenCharge, Currency, aiEditsUsed, VersionDraft + миграция балансов (всем 500) | ✅ |
| 16.3 | Библиотека списаний `token-charges.ts` (charge/refund/quota, FOR UPDATE) | ✅ |
| 16.4 | Регистрация: бонус 500 токенов | ✅ |
| 16.5 | Списание за генерацию + автовозврат при фейле воркера | ✅ |
| 16.6 | Чат: пакеты правок (10 на оплату) вместо лимита 20/версию | ✅ |
| 16.7 | API квоты и докупки пакета правок | ✅ |
| 16.8 | «Переписать заново» загруженный документ (REWRITE) | ✅ |
| 16.9 | Списания за проверку рисков и анализ загрузки + возвраты | ✅ |
| 16.10 | Демонтаж постоплаты: purchase API/UI, водяные знаки, гейты скачивания/печати/копирования | ✅ |
| 16.11 | UI токенов: баланс, история, счётчик пакета, цены на кнопках | ✅ |
| 16.12 | Настоящий серверный черновик (VersionDraft) | ⬜ |
| 16.13 | Редактируемый предпросмотр (TipTap + тулбар) | ⬜ |
| 16.14 | Слой оформления: воркер генерирует только тело | ⬜ |
| 16.15 | API decor + шаг «Оформление» перед скачиванием | ⬜ |
| 16.16 | Единая сборка слоёв: предпросмотр, шаринг, сравнение, DOCX | ⬜ |

---

## Иконки статусов

| Иконка | Значение |
|--------|---------|
| ⬜ | Не начата |
| 🔄 | В работе (укажи имя: 🔄 Паша) |
| ✅ | Готово |
| 🚫 | Заблокирована (нужна другая задача сначала) |

---

*Последнее обновление: 2026-08-03 (фаза 15 — правовые документы и согласия)*
