# Итоговый отчёт: аудит, фиксы и статус «Догодок»

> Дата: 2026-07-30. Составлен по итогам полного аудита (код + живой прогон прода app.dogodoc.ru) и серии исправлений, задеплоенных в прод.

---

## 1. Что сделано за сессию (коротко)

- Проведён полный аудит: код (4 агента), живой прогон прода, аудит ИИ-промптов и настроек.
- Найдено и **исправлено + задеплоено в прод 7 багов** (в т.ч. критические: перевёрнутые стороны, обрыв договоров, логаут активного юзера).
- Проведён A/B ИИ-моделей на живых полноразмерных договорах → выбрана оптимальная модель (**DeepSeek V4 Flash**, ~0,30 ₽/договор).
- Подготовлены коммерческие документы: бизнес-план, план продвижения, экономика моделей.

---

## 2. Баги ИСПРАВЛЕНЫ и ЗАДЕПЛОЕНЫ на прод ✅

| # | Баг | Коммит | Влияние |
|---|-----|--------|---------|
| 1 | **Обрыв договоров** — жадная регулярка вырезала весь текст после слова «реквизиты» (инлайновое «банковские реквизиты»). Договоры выходили ~7k знаков вместо 30k | `0db267b` | **Критический.** Теперь полноразмерные договоры (проверено 33k знаков) генерируются целиком, все 11 разделов |
| 2 | **Роль сторон** захардкожена `'executor'` — любой договор «от Заказчика» шёл с перевёрнутыми сторонами | `d4e878b` | **Критический.** Проверено: выбор «Заказчик» → в договоре Заказчик |
| 3 | **Логаут активного юзера** — при истечении 15-мин токена middleware кидал на /login, хотя refresh-токен (30 дней) жив | `d08bd9e` | Не выбрасывает посреди работы |
| 4 | **Объём/max_tokens** занижен + нет требования завершать разделы | `d4e878b` | Модель дописывает все разделы |
| 5 | **Сохранение операторов** — галочка «Включён» в /admin/ai не сохранялась (кнопка была спрятана) | `d4e878b` | Можно отключать операторов (сделали Polza-only) |
| 6 | **401 роняет автосейв/правки** на рабочем экране (сырой fetch мимо apiFetch) | `cc9d927` | Автосейв/правки/генерация не падают на истечении токена |
| 7 | **401 на всех страницах** (админка, баланс и т.д. — сырой fetch) | `db8da79` | Глобальный авто-refresh сессии при 401 |
| 8 | **Пустое приложение** — генерация приложений/ДС на DeepSeek возвращала пустой результат | воркер + Qwen на генерацию | Приложения/ДС генерируются; воркер не сохраняет пустой черновик |
| 9 | **Правки/чат/проверка приложений и ДС не работали** — тело обрезалось в пустоту. `REQS_HEADER_RE` матчил «Реквизиты документа» (первый раздел приложений) → `splitRequisitesBlock` считал весь документ подвалом | `2d62f22` | **Критический.** Теперь правки/вопросы/проверка приложений и ДС работают |
| 10 | **Правки таблиц падали** — модель возвращала только строку `<tr>`, валидатор её отклонял | `ea43c5a` | Отдельный путь правки таблиц: цена в ячейке меняется, сумма пересчитывается, структура таблицы цела |

---

## 3. Что проверено вживую на проде ✅

- **Генерация полноразмерного договора** (33k знаков) — все 11 разделов + реквизиты, без обрыва.
- **Роль сторон** — корректна (Заказчик = пользователь, если выбран Заказчик).
- **Правка на большом договоре** — изменил только п.6.1 в 33k-документе, остальное нетронуто (движок берёт релевантные блоки, не теряет контекст).
- **Сверка версий** (diff) — работает, пословно как в Word.
- **Отключение операторов** GigaChat/OpenRouter — сохраняется, система на Polza-only.
- **Переключение ИИ-моделей** в /admin/ai — работает, маршруты сохраняются.
- **Привязка приложения к договору** — приложение создаётся с ссылкой «№ 1 к <договор>».
- **Приложение строится на ТЕКСТЕ корневого договора (контекст)** — подтверждено: сгенерированное приложение прямо ссылается на пункты родителя («…является неотъемлемой частью Договора поставки оборудования… В соответствии с пунктом 1.7 Договора…»), использует его стороны и предмет (станок XYZ-500). ИИ понимает контекст корневого документа. ✅
- **Правка большого договора (33k)** — изменил только п.6.1 (0,3%→0,5%), остальное не задето, создана v.2. ✅
- **Чат «Вопрос»** — ИИ даёт документ-специфичный ответ (уровень защиты пункта, ст. 333 ГК РФ, расчёты), документ не меняется. ✅
- **Проверка рисков (review)** — оценка 76/100, замечания со ссылками на пункты и категориями. Работает на Qwen3.6 Plus (на DeepSeek падала с JSON-ошибкой → переключил). ✅
- **Реальная экономика** из usage-лога (см. `07-Экономика-и-выбор-ИИ-моделей.md`).

---

## 4. Баги НАЙДЕНЫ, но НЕ исправлены (требуют работы)

### Критично для боевого запуска
| # | Баг | Где | Приоритет |
|---|-----|-----|-----------|
| ~~A1~~ | **РЕШЕНО.** «Приложение генерируется пустым» — оказался баг **DeepSeek V4 Flash на дочерних документах** (reasoning-модель отдаёт пустой `content`). Проверено: то же приложение на **Qwen3.6 Plus → 15 605 знаков** со ссылками на пункты договора. **Фикс:** генерация переключена на Qwen3.6 Plus (надёжна для договоров И приложений) + воркер теперь не сохраняет пустой черновик, а повторяет задачу (коммит `e33535b`) | генерация → Qwen; воркер | ✅ Исправлено |
| A2 | **Покупка версии без настоящего `SELECT FOR UPDATE`** — при параллельных покупках баланс уходит в минус | `api/versions/[id]/purchase/route.ts:53-83` | Высокий (деньги) |
| A3 | **Утечка ПДн в логи** — `console.log` пишет фрагменты договоров/промптов в `logs/app.log` | `gigachat-provider.ts:769`, `chat/route.ts:99` | Высокий (152-ФЗ) |
| A4 | `next.config.ts` **глушит ошибки сборки** (`ignoreBuildErrors`) + нет `prisma generate` в CI | `next.config.ts:31-32` | Средний (на проде админка работает, но CI слепой) |

### Функциональные / данные
- **Преамбула:** пустое имя Исполнителя (в реквизитах внизу корректно) + enum основания «CERTIFICATE» не переведён на русский.
- **IDOR** `/api/jobs/:jobId` — можно опрашивать чужие задачи (влияние низкое).
- **Несуществующие поля** `profile.ogrnDate`, `poaNumber`, `profile.email` — typecheck-ошибки, дата ОГРН и email в DOCX пусты.
- **«Создать приложение»** не преднаполняет контрагента/родителя автоматически (надо выбрать контрагента вручную).

### UI / кнопки (из аудита кнопок)
- **9 кнопок-пустышек:** тарифы хранилища «Перейти»/«Добавить», факсимиле/печать, «Забыли пароль?», «Новый документ» (primary) в карточке контрагента, кнопка-щит защиты, логаут на несуществующий `/auth`.
- **Битый роут `/profile/requisites`** (404) в сценарии загрузки документа (правильный `/requisites`).
- **Хардкод «540 ₽»** на балансе/главной расходится с реальной ценой (`calcVersionPrice` = 40–100 ₽).
- Мелочи: аватар-инициалы «О«», «ИП ООО …» в превью, мислейбл «Свернуть»→«Изм.».

### Дизайн (из аудита дизайна)
- **~12 экранов рендерят сериф системным шрифтом** (Georgia/Times) вместо Source Serif 4 — токен `--font-serif` не определён, надо `--font-display`.
- Статус «Оплачено» зелёный вместо slate-indigo; approved/paid/signed не различаются.
- **Адаптивность ≤768px** сделана только для каркаса — внутренние страницы (balance, storage, payments, requisites, counterparties, templates, history) без брейкпоинтов.
- Доступность: мало `aria-label`, `focus-visible` только у Button.

### Нагрузка (из аудита масштабируемости)
- **Нет индексов БД** на `userId` и др. → seq scan на списках.
- **Воркер `concurrency=1`** — 1 генерация за раз на всю систему.
- **Нет rate-limit** на generate/chat/review/topup/upload (только login/register).
- **STORAGE_DIR** не смонтирован как общий volume — файлы теряются при redeploy.
- N+1 в counterparties, нет пагинации в history.
Полный расчёт ёмкости и приоритеты — в `01-Аудит-системы.md`.

---

## 5. Что ещё НЕ проверено (нужно проверить дальше)

Осталось за рамками этой сессии (браузерные тесты / отдельная проработка):

1. ~~Приложение генерируется пустым~~ — **РЕШЕНО** (баг DeepSeek на child-doc; генерация на Qwen работает, контекст родителя учитывается). Приложение из системы протестировано ✓.
2. ~~Допсоглашение (AMENDMENT)~~ — **ПРОВЕРЕНО** ✅ ДС на основе загруженного договора ООО «Медикал сити групп»: шапка/преамбула как у договора (роли верны: ИП Савинов = Исполнитель, ООО = Заказчик), смета-таблица с фото/видеосъёмкой и суммами, блок «Реквизиты и подписи сторон» с реквизитами обеих сторон. **Компактность починена:** было 18 642 знака → стало 8 131 знак (коммиты `4acbbb4` шапка/структура, `0602cc9` компактность + фикс двойного префикса «Общество… ООО»). Себест. 1,57 ₽.
3. **Приложение/ДС через «Загрузить файл»** — когда основной договор НЕ в системе: пользователь загружает договор, ИИ его читает и делает приложение. Не тестировалось (в мастере есть вкладка «Загрузить файл» в блоке «Основной договор»).
4. **Поведение при удалённом основном договоре** — пользователь пересоздаёт/загружает корень. Не тестировалось.
5. ~~Проверка рисков~~ — **ПРОВЕРЕНО** ✅ (работает на Qwen; на DeepSeek падала — переключено).
6. ~~Режим «Вопрос»~~ — **ПРОВЕРЕНО** ✅ (документ-специфичные ответы, текст не меняется).
7. **Извлечение сторон при загрузке** (extract_parties) + согласие на ПДн. Не тестировалось (переключено на Qwen на всякий случай — структурная JSON-задача).
8. ~~Покупка версии + скачивание DOCX~~ — **ПРОВЕРЕНО КЛИКАМИ** ✅ Покупка ДС №3: баланс 4930 → 4890 ₽ (−40 ₽), создана транзакция «Покупка версии», статус версии → «Оплачено». Скачивание: `GET /api/versions/:id/download` → 200 (валидный файл). Повторное бесплатное скачивание купленной версии — работает. ⚠️ **Два бага (см. ниже).**
9. ~~Страницы баланс/платежи/хранилище/история/документы~~ — **ПРОВЕРЕНО КЛИКАМИ** ✅ Все рендерятся и работают: Баланс (4890 ₽ + операции), История платежей (группировка по датам, пополнено/потрачено), Хранилище (529 КБ/10 МБ, разбивка по типам, тарифы), История версий (фильтры по типу/дате работают), Документы (12 док., фильтры, поиск, дерево «родитель→вложения»: под договором MSG вложены 3 ДС).

### 🔴 КРИТИЧЕСКИЙ баг скачивания (01.08.2026) — ИСПРАВЛЕН И ЗАДЕПЛОЕН ✅
**Симптом:** скачанный Word содержал не весь договор и БЕЗ приложений (у пользователя: 9 003 знака вместо ~37 000, приложений нет).

**Причина:** `splitRequisitesBlock` брал ПЕРВОЕ совпадение с `REQS_HEADER_RE`. В договоре раздел **«13. Место нахождения и банковские реквизиты Сторон»** матчился как «подвал документа» → всё после него отрезалось: **17 271 знак, 3 приложения, 6 таблиц**. Плюс в файл попадала вторая (системная) шапка — дубль.

**Фикс** (коммит `13d36b7`):
- `hasSectionsAfter()`: подвалом считается только настоящий ХВОСТ документа. Если после найденного заголовка идут новые разделы/приложения — ищем дальше. (Кириллица: `\b` в JS-regex не работает — lookahead.)
- Скачивание: для ЗАГРУЖЕННЫХ документов не подставляем системные шапку/реквизиты — у них свои (убирает дубль шапки и лишнюю обрезку).

**Проверка на проде (все 4 оплаченных документа скачаны и распакованы):**
| Документ | Текста в DOCX | Полнота | Приложения | Таблиц |
|---|---|---|---|---|
| Договор с РООУ | 37 101 (было 9 003) | ✅ | №1, №2, № 3 | 6 |
| Договор (крупный) | 46 294 из 46 298 | **100%** | №1, №2, № 3 | 9 |
| Приложение | 13 564 | **100%** | — | 5 |
| Приложение | 8 497 | **100%** | — | 4 |

> ⚠️ Урок на будущее: любые «умные» обрезки контента (подвал/преамбула) обязаны проверять, что после точки реза действительно ничего значимого нет. Это уже третий баг этого класса (ранее: обрыв договоров по слову «реквизиты», поломка правок приложений).

### Оформление выгрузки в Word (02.08.2026) — ИСПРАВЛЕНО ✅
По фидбэку на скачанном файле:
- **Реквизиты шли одним столбиком.** Причина: реквизиты из загруженного Word приходят как `div.doc-layout-table` (mammoth разворачивает layout-таблицу), а конвертер обрабатывал только системный класс `doc-requisites` → колонки схлопывались. Теперь оба класса рендерятся 2-колоночной таблицей без рамок. Дополнительно `groupRequisitesColumns` группирует реквизиты, идущие простыми абзацами («Заказчик:» … «Исполнитель:» с ИНН/ОГРН), — на случай документов без таблицы.
- **Приложения не начинались с новой страницы.** Теперь «Приложение № N» / «Дополнительное соглашение № N» получают `pageBreakBefore` и оформляются как заголовок части (по центру, жирным). Перед первым блоком документа разрыв не ставится. В предпросмотре заголовки приложений тоже распознаются (h2).

**Проверка на проде (4 оплаченных документа, файлы скачаны и распакованы):**
| Документ | Подписи/реквизиты | Полнота текста | Разрывов страниц |
|---|---|---|---|
| Договор РООУ | **1×2 (две колонки)** | 100% | 3 (перед каждым приложением) |
| Договор крупный | **1×2** | 100% | 3 |
| Приложение A | **1×2** | 100% | 0 (само является приложением — верно) |
| Приложение B | **1×2** | 100% | 0 |

### Баги, найденные при живом клик-прогоне (31.07.2026) — ВСЕ ИСПРАВЛЕНЫ И ЗАДЕПЛОЕНЫ ✅
Коммит `Фиксы после клик-прогона` (деплой 30628966105, success). Перепроверено кликами на проде.

| # | Баг | Фикс | Проверка |
|---|-----|------|----------|
| B1 | **Баланс в шапке не обновлялся после покупки** — висело старое значение (топбар из `authStore`, ставился один раз при `initialize`). | purchase-роут возвращает новый `balance`; `purchaseVersion()` и пополнение вызывают `authStore.setBalance`. | ✅ Покупка ДС №2: шапка моментально 4890 → 4850 ₽ без перезагрузки. |
| B2 | **Нет модалки подтверждения покупки** — списывало сразу по клику. | Добавлена модалка (стоимость / баланс / останется; ветка «недостаточно средств» → «Пополнить баланс»). | ✅ Клик «Купить» открывает модалку, а не списывает сразу. |
| B3 | **Хардкод «540 ₽»** на Балансе и главной (реальные цены 40–100 ₽). | Реальные цены: «Договор 50–100 ₽», «Приложение / ДС 40–100 ₽»; оценка «хватит на N» по 60 ₽. | ✅ На `/balance` больше нет 540. |
| B4 | **Кнопки тарифов «Перейти»/«Добавить» — пустышки**. | Помечены «Скоро» + `disabled` (платных тарифов нет в MVP). | ✅ Кнопки серые, неактивные, тултип «появятся позже». |
| A2 | **Покупка без настоящего `SELECT FOR UPDATE`** — при гонке баланс мог уйти в минус (ранее «эмуляция», строка не блокировалась). | В транзакции `SELECT balance FROM wallets WHERE id=… FOR UPDATE` — реальная блокировка строки кошелька. | ✅ Покупка работает; списание корректно. |

### Остальные разделы меню — ПРОВЕРЕНО КЛИКАМИ (31.07.2026) ✅
- **Главная** — приветствие по времени, карточки действий, недавние документы (статусы актуальны), баланс/хранилище-виджеты. Работает.
- **Контрагенты** — список, поиск, карточка (статистика, вкладки «Все документы»/«Реквизиты»), реквизиты (организация, банк, контакты, подписанты), drawer подписанта (ФИО/должность/основание + живое превью строки). Работает. **Найдено и исправлено 2 UI-бага** (коммит `Контрагенты: фикс аватар-инициалов…`, деплой 30631061453):
  - аватар-инициалы захватывали кавычку («О«» вместо «М») → теперь ООО «Медикал»→МС, АНО «Центр»→ЦР, ИП Хубелашвили→ХВ;
  - подпись юр-формы под именем всегда была «ООО» (по длине ИНН) → теперь берётся из названия: АНО→АНО, Ассоциация→Организация, ИП→ИП.
- **Шаблоны** — список, «Загрузить шаблон», меню (Использовать/Просмотреть/Переименовать/Удалить), модалка предпросмотра «только чтение» с полным текстом. Работает.
- **Мои реквизиты / Мой профиль** (`/requisites`) — вкладка «Аккаунт» (имя/email/смена пароля) + «Реквизиты компаний» (несколько профилей ИП/ООО, полная форма, превью «Вид в договоре»). Работает. Мелочь: поля «Дата ОГРНИП» и Email в профиле пустые (незаполненные опциональные, не баг).

Мелкие косметические (не чинил, низкий приоритет): «Контрагент с **июль** 2026» (падеж) в карточке; в превью подписанта-женщины «действующ**ий**» вместо «действующая» (согласование рода).

### Загрузка документа `/documents/upload` — ПРОВЕРЕНО (эндпоинты) ✅
Файловый пикер драгом не тестировался (ограничение инструмента), но весь бэкенд-пайплайн, который вызывает страница, прогнан напрямую тем же payload’ом:
- **`/api/documents/analyze`** (проверка рисков, SSE) → 200, оценка 32/100 для намеренно кабального к Заказчику договора, **12 структурированных замечаний** (100% предоплата, односторонний разрыв Исполнителем, подсудность по месту Исполнителя, размытый предмет). Анализ ролевой (роль Заказчик учтена).
- **`/api/documents/extract-parties`** (извлечение сторон, только с согласием ПДн) → 200, точно распознаны ИНН/ОГРН/КПП/адреса обеих сторон и **роли** (ИП→executor, ООО→customer).
- Страница рендерится корректно: дропзона, выбор роли, чекбокс согласия на ПДн (без согласия риски всё равно считаются — верно по дизайну).
- ⚠️ Замечен транзиентный **502** на запросе, совпавший с рестартом контейнера при деплое — при повторе на чистом контейнере всё 200. Не баг приложения.
10. **Нагрузка** — только расчёт по коду, реальной стрельбы не было.
11. **Review в BullMQ** — сейчас синхронный, ~2,5–3 мин на 33k-договор, риск таймаута nginx 300с. Кандидат на вынос в очередь.

---

## 6. Рекомендованный порядок дальнейшей работы

1. **Починить приложение (A1)** — ключевая функция, сейчас пустой результат. Возможно, для child-docs временно оставить Qwen3.6 Plus, пока не разобран DeepSeek на этой ветке.
2. **Деньги/ПДн:** настоящий `SELECT FOR UPDATE` в покупке (A2) + убрать `console.log` с ПДн (A3).
3. **Дотестировать** пп. 2–8 из раздела 5 (допсоглашение, загрузка-база, риски, вопрос, покупка, скачивание).
4. **Нагрузка перед рекламой:** индексы БД, `WORKER_CONCURRENCY`, rate-limit, общий volume.
5. **Полировка:** токен `--font-serif`, «мёртвые» кнопки, хардкод 540 ₽, адаптивность, битый роут /profile/requisites.

---

## 7. Карта документов пакета

- `00-README.md` — индекс
- `01-Аудит-системы.md` — техаудит (блокеры, риски, нагрузка, кнопки, дизайн)
- `02-Бизнес-план.md` — прайсинг, монетизация, платежи, прогноз
- `03-План-продвижения.md` — маркетинг и PR
- `04-Аудит-ИИ-промптов-и-настроек.md` — промпты, объём, ползунки, экономика моделей
- `05-План-работ-и-выбор-ИИ-моделей.md` — план работ + протокол выбора моделей
- `06-Итоговый-отчёт-и-статус.md` — **этот файл** (что найдено/исправлено/осталось)
- `07-Экономика-и-выбор-ИИ-моделей.md` — реальные цифры экономики + выбор модели
